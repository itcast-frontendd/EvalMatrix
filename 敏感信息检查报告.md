# 🔒 EvalMatrix 项目敏感信息检查报告

## ⚠️ 发现的高风险敏感信息

### 1. API Keys（严重 - 必须移除）

| 文件 | 敏感内容 | 风险等级 |
|-----|---------|---------|
| `backend/.env` | OpenRouter API Key: `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` | 🔴 严重 |
| `backend/.env` | Dify API Key: `app-59J7VxBA4PeMEB9UeXnGRWhh` | 🔴 严重 |
| `backend/.env` | 网易 AI Gateway Key: `thw0hqx4qli9ndax.u8xvo6a400cbqtatey8h7sq18ah8hfsc` | 🔴 严重 |
| `backend/judge_config.json` | API Key: `831nu8vxwh9otek3.yco18hmkgb0tpayykqi6s47j8cvc9trj` | 🔴 严重 |
| `backend/product_config.json` | 多个 API Keys | 🔴 严重 |

### 2. 内部系统信息（中等风险）

| 信息类型 | 位置 | 说明 |
|---------|------|------|
| 内部 Dify URL | `backend/.env` | `https://luna-dify.inner.youdao.com` - 网易内部系统 |
| 内部 AI Gateway | 多处 | `https://aigw.netease.com` - 网易内部网关 |

### 3. 业务数据文件（需要排除）

| 文件夹/文件 | 大小 | 说明 |
|------------|------|------|
| `backend/outputs/` | 大量 | 评测输出数据 |
| `backend/uploads/` | 大量 | 用户上传的评测数据 |
| `eval_results/` | 大量 | 评测结果历史数据 |
| `aligned_output/` | 中等 | 对齐后的数据 |
| `*.xlsx` | 多个 | Excel 数据文件 |
| `*.zip` | 多个 | 压缩数据包 |
| `*.jsonl` | 多个 | JSON Lines 数据 |

### 4. 依赖文件夹（无需上传）

| 文件夹 | 说明 |
|-------|------|
| `.venv/` | Python 虚拟环境 |
| `frontend/node_modules/` | Node.js 依赖 |

---

## 🛡️ 清理方案

### 步骤 1：创建 .gitignore 文件

```gitignore
# ============================================================================
# 敏感配置文件
# ============================================================================
.env
.env.local
.env.*.local
judge_config.json
product_config.json
*.key
*.pem
*.secret

# ============================================================================
# 数据文件
# ============================================================================
*.xlsx
*.xls
*.csv
*.zip
*.jsonl
aligned_output/
eval_results/
backend/outputs/
backend/uploads/

# ============================================================================
# Python
# ============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
env/
venv/
*.egg-info/
dist/
build/

# ============================================================================
# Node.js
# ============================================================================
node_modules/
.next/
out/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ============================================================================
# IDE
# ============================================================================
.vscode/
.idea/
*.swp
*.swo
*~

# ============================================================================
# OS
# ============================================================================
.DS_Store
Thumbs.db
desktop.ini

# ============================================================================
# 日志文件
# ============================================================================
*.log
logs/
```

### 步骤 2：备份并删除敏感文件

```powershell
# 创建备份文件夹
mkdir D:\EvalMatrix-敏感信息备份

# 备份敏感文件
copy "backend\.env" "D:\EvalMatrix-敏感信息备份\.env.backup"
copy "backend\judge_config.json" "D:\EvalMatrix-敏感信息备份\judge_config.json.backup"
copy "backend\product_config.json" "D:\EvalMatrix-敏感信息备份\product_config.json.backup"

# 删除敏感文件（从 Git 历史中移除）
del "backend\.env"
del "backend\judge_config.json"
del "backend\product_config.json"
```

### 步骤 3：创建配置模板文件

创建 `backend/.env.example`：

```env
# ============================================================================
# Judge 配置 (使用网易 AI Gateway 或 OpenRouter)
# ============================================================================
JUDGE_API_URL=https://aigw.netease.com/v1/chat/completions
JUDGE_API_KEY=your-api-key-here
JUDGE_MODEL=gemini-2.5-flash

# 可选：OpenRouter 配置
# JUDGE_API_URL=https://openrouter.ai/api/v1/chat/completions
# JUDGE_API_KEY=sk-or-v1-your-key-here
# JUDGE_MODEL=anthropic/claude-3-5-sonnet-20241022

JUDGE_MAX_INPUT_TOKENS=200000
JUDGE_MAX_OUTPUT_TOKENS=64000
JUDGE_RPM_LIMIT=120
JUDGE_TPM_LIMIT=200000
JUDGE_CONCURRENCY=3
JUDGE_TIMEOUT=90

# ============================================================================
# 可选：Dify 配置
# ============================================================================
# DIFY_API_URL=https://your-dify-instance.com/v1/workflows/run
# DIFY_API_KEY=your-dify-key-here
```

创建 `backend/judge_config.example.json`：

```json
{
  "api_key": "your-api-key-here",
  "model": "gemini-2.5-flash",
  "api_url": "https://aigw.netease.com/v1/chat/completions",
  "max_input_tokens": 200000,
  "max_output_tokens": 64000,
  "concurrency": 3,
  "timeout": 300,
  "batch_api_enabled": false,
  "prompt_cache_enabled": false
}
```

### 步骤 4：修改代码中的硬编码密钥

检查以下文件中的硬编码 API Keys：

1. `align_with_llm.py` - 第 35 行有硬编码的 API Key
2. 其他 Python 文件中的 `LLM_CONFIG`

需要将这些改为从环境变量读取。

---

## 🚀 安全的 GitHub 上传步骤

### 第 1 步：初始化 Git

```powershell
cd "D:\POPO\AItester0227\AItester0227 - latest"

# 初始化 Git
git init

# 创建 .gitignore（上面的内容保存为 .gitignore 文件）
```

### 第 2 步：清理敏感文件

```powershell
# 删除敏感文件
del "backend\.env"
del "backend\judge_config.json"
del "backend\product_config.json"

# 删除数据文件夹（可选，如果不需要保留历史数据）
# Remove-Item -Recurse -Force "eval_results"
# Remove-Item -Recurse -Force "aligned_output"
```

### 第 3 步：添加文件到 Git

```powershell
# 添加所有文件（.gitignore 会自动排除敏感文件）
git add .

# 检查将要提交的文件
git status

# 确保没有敏感文件被包含！
```

### 第 4 步：提交并推送

```powershell
# 提交
git commit -m "Initial commit: EvalMatrix - AI-driven product evaluation system

- Add complete source code
- Add configuration templates
- Exclude sensitive data and API keys"

# 添加远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/EvalMatrix.git

# 推送
git branch -M main
git push -u origin main
```

---

## ✅ 上传前检查清单

- [ ] `.gitignore` 已创建并包含所有敏感文件模式
- [ ] `backend/.env` 已删除或已添加到 .gitignore
- [ ] `backend/judge_config.json` 已删除或已添加到 .gitignore
- [ ] `backend/product_config.json` 已删除或已添加到 .gitignore
- [ ] 创建了 `.env.example` 和 `judge_config.example.json` 模板
- [ ] 运行 `git status` 确认没有敏感文件被跟踪
- [ ] 代码中的硬编码 API Keys 已改为环境变量读取
- [ ] README 中添加了配置说明

---

## 📝 README 配置说明模板

在 README.md 中添加：

```markdown
## ⚙️ 配置

### 1. 后端配置

复制配置模板：
```bash
cp backend/.env.example backend/.env
cp backend/judge_config.example.json backend/judge_config.json
```

编辑配置文件，填入你的 API Keys：
- `backend/.env` - 环境变量配置
- `backend/judge_config.json` - Judge 模型配置

### 2. 支持的模型

- OpenAI 兼容 API（OpenRouter、文心一言等）
- 网易 AI Gateway
- Dify Workflow
```

---

## ⚠️ 重要提醒

1. **永远不要提交真实的 API Keys 到 GitHub**
2. **如果已经提交，立即撤销并更换 API Keys**
3. **使用环境变量或配置文件（在 .gitignore 中）**
4. **提供配置模板（.example 文件）供用户参考**

---

*报告生成时间：2026-04-07*
